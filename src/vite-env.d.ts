/// <reference types="vite/client" />

/**
 * This directive tells TypeScript to include the Vite client types
 * for proper type checking and IntelliSense support when using Vite.
 *
 * It ensures that Vite-specific types such as environment variables
 * and other client-side APIs are correctly typed in your project.
 */
